This is a Pd library called "cor"

== INSTALL ==

Type "make"

The external object is called color_class

Prior to creating such an object, place it somewhere in Pd's search path, then
use the "-lib cor" flag to load it
